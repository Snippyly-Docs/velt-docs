---
title: "Setup"
---

<Tabs>
  <Tab title="React">
    ### 1) Import the `VeltUserInviteTool` from `@veltdev/react`
    Import the component

    ### 2) Place the VeltUserInviteTool component
    Place the component wherever you want the invite button to appear.

  </Tab>
  <Tab title="Next.js">
    ### 1) Import the `VeltUserInviteTool` from `@veltdev/react`
    Import the component

    ### 2) Place the VeltUserInviteTool component
    Place the component wherever you want the invite button to appear.


  </Tab>
  <Tab title="HTML">
    ### 1) Import the <velt-user-invite-tool> component
    Import the component

    ### 2) Place the <velt-user-invite-tool>
    Place the component wherever you want the invite button to appear.


  </Tab>
</Tabs>

<RequestExample>

```java React
import { VeltUserInviteTool } from '@veltdev/react';

function YourComponent() {

  return (
		...
    <VeltUserInviteTool />
    ...
  )
  
}
```

```java Next.js
import { VeltUserInviteTool } from '@veltdev/react';

function YourComponent() {

  return (
		...
    <VeltUserInviteTool />
    ...
  )
  
}
```

```html HTML
<velt-user-invite-tool></velt-user-invite-tool>
```

</RequestExample>
