## Setting a Document ID

You can set a user's the `Document ID` by using the `client.setDocumentId` method:

```jsx
let client = useVeltClient()
client.setDocumentId('unique-document-id')
```

## Hooks

You can also use one of our SDK's custom hooks to set the ID:

TODO