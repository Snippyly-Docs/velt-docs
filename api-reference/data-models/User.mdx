{/* <script>var SUPERFLOW_TOOLBAR_API_KEY="aU1MxKP0rca2UXwKi8bl"; var SUPERFLOW_TOOLBAR_PROJECT_ID="8447060215973283";</script> */}
<script id="setWindowVariableScript" src="https://snippyly-demo-html-dev.web.app/set-window-variable.js?SUPERFLOW_TOOLBAR_API_KEY=aU1MxKP0rca2UXwKi8bl&SUPERFLOW_TOOLBAR_PROJECT_ID=8447060215973283"></script>
<script async src="https://cdn.jsdelivr.net/npm/@usesuperflow/toolbar/superflow.min.js"></script>

## `User` Class Properties

| Property         | Type          | Optional | Description                                                                                               |
|------------------|---------------|----------|-----------------------------------------------------------------------------------------------------------|
| `userId`         | string        | No       | Unique user identifier used to identify your user.                                                        |
| `name`           | string        | Yes      | The full name of your user. Defaults to a random avatar name if not provided.                             |
| `photoUrl`       | string        | Yes      | The display picture URL of your user. Defaults to a random avatar image if not provided.                  |
| `email`          | string        | Yes      | Required for sending email or Slack notifications to users about comments and mentions.                   |
| `contacts`       | UserContact[] | Yes      | A list of contacts to show in a dropdown when a user wants to tag co-workers in a comment.                |
| `plan`           | string        | Yes      | The product plan the user is on.                                                                          |
| `groupId`        | string        | Yes      | A domain name or identifier used to cluster a group of users who work together.                           |
| `clientGroupId`  | string        | Yes      | The original groupId provided by the user.                                                                |
| `userSnippylyId` | string        | Yes      | Auto-generated unique user ID linked with your app's user ID, needed for internal processing.             |
| `color`          | string        | Yes      | A random color assigned to the user for the current session, used for avatar border and live cursor.      |
| `type`           | string        | Yes      | The type of user.                                                                                         |
| `isAdmin`        | boolean       | Yes      | Indicates if the user has administrative privileges.                                                      |
| `isReadOnly`     | boolean       | Yes      | Indicates if the user has read-only access.                                                               |
| `isAnonymous`    | boolean       | Yes      | Indicates if the user is anonymous and can only view comments.                                            |
| `isGuest`        | boolean       | Yes      | Indicates if the user is a guest.                                                                         |                                                          |

## `UserOptions` Class Properties

| Property        | Type    | Optional | Description                                                                      |
|------------------|---------|----------|-----------------------------------------------------------------------------------|
| `replaceContacts`| boolean | Yes      | If set to true, it will replace the user's personal and group contacts with the ones provided. |
| `authToken`      | string  | Yes      | The authentication token of the user.                                             |

