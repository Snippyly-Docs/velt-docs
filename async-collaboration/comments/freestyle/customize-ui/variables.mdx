---
title: "3. Variables"
---


<Tabs>
<Tab title="Pin">
## 1) Select the Comment Pin component

You can select the Comment Pin component.
Our CSS variables are set at the component level.

## 2) Modify a CSS variable
Set the variable to anything you want.
We expose a set of variables so you can customize the component to better match your UI.

## 3) Check out the table below

Reference the table below to see what parts we expose.
Alternatively, you can directly inspect the component HTML to see what parts are available.

<ResponseField name="--velt-comment-pin-size" default="2.5rem">
  Sets the comment pin size
</ResponseField>
<ResponseField name="--velt-comment-pin-avatar-size" default="1.5rem">
  Sets the comment pin avatar size
</ResponseField>
<ResponseField name="--velt-comment-pin-bg-color" default="var(--velt-purple)">
  Sets the comment pin background color
</ResponseField>
<ResponseField name="--velt-comment-pin-border-radius" default="6.5em 6.5em 6.5em 0.125em">
  Sets the comment pin border radius
</ResponseField>
<ResponseField name="--velt-comment-pin-box-shadow" default="0px 8px 16px -8px rgba(15, 15, 15, 0.1)">
  Sets the comment pin box shadow
</ResponseField>
<ResponseField name="--velt-comment-pin-ghost-color" default="var(--velt-grey-3-color)">
  Sets the comment pin ghost color
</ResponseField>
<ResponseField name="--velt-comment-pin-popover-color" default="#a259fe">
  Sets the comment pin popover color
</ResponseField>
<ResponseField name="--velt-comment-pin-border" default="2px solid #FFFFFF">
  Sets the comment pin border
</ResponseField>
<ResponseField name="--velt-comment-pin-border-width" default="0 15px 15px 0">
  Sets the comment pin border width
</ResponseField>
<ResponseField name="--velt-comment-pin-border-color" default="transparent var(--velt-comment-pin-popover-color) transparent transparent">
  Sets the comment pin border color
</ResponseField>
<ResponseField name="--velt-comment-pin-index-border" default="1px solid rgb(210, 210, 210)">
  Sets the comment pin index border
</ResponseField>
<ResponseField name="--velt-comment-pin-index-font-size" default="0.55rem">
  Sets the comment pin index font size
</ResponseField>
<ResponseField name="--velt-comment-pin-index-bg" default="white">
  Sets the comment pin index background color
</ResponseField>
<ResponseField name="--velt-comment-pin-index-color" default="black">
  Sets the comment pin index color
</ResponseField>
<ResponseField name="--velt-internal-lock-icon-color" default="white">
  Sets the comment pin lock icon color
</ResponseField>
<ResponseField name="--velt-internal-lock-bg-color" default="grey">
  Sets the comment pin lock background color
</ResponseField>
<ResponseField name="--velt-comment-pin-bubble-size" default="clamp(20px, 24px, 30px)">
  Sets the comment pin bubble size
</ResponseField>
<ResponseField name="--velt-comment-pin-bubble-border-radius" default="50%">
  Sets the comment pin bubble border radius
</ResponseField>
<ResponseField name="--velt-comment-pin-bubble-offset-top" default="-8px">
  Sets the comment pin bubble offset top
</ResponseField>
<ResponseField name="--velt-comment-pin-bubble-offset-right" default="-8px">
  Sets the comment pin bubble offset right
</ResponseField>
<ResponseField name="--velt-comment-pin-bubble-offset-left" default="unset">
  Sets the comment pin bubble offset left
</ResponseField>
<ResponseField name="--velt-comment-pin-bubble-offset-bottom" default="unset">
  Sets the comment pin bubble offset bottom
</ResponseField>


</Tab>

<Tab title="Bubble">
## 1) Select the Comment Pin component

You can select the Comment Pin component.
Our CSS variables are set at the component level.

## 2) Modify a CSS variable
Set the variable to anything you want.
We expose a set of variables so you can customize the component to better match your UI.

## 3) Check out the table below

Reference the table below to see what parts we expose.
Alternatively, you can directly inspect the component HTML to see what parts are available.

<ResponseField name="--velt-comment-bubble-container-gap" default="0">
  Sets the comment bubble container gap
</ResponseField>
<ResponseField name="--velt-comment-bubble-count-icon-stroke-width" default="2">
  Sets the comment bubble count icon stroke width
</ResponseField>
<ResponseField name="--velt-comment-bubble-count-container-height" default="26px">
  Sets the comment bubble count container height
</ResponseField>
<ResponseField name="--velt-comment-bubble-avatar-size" default="var(--velt-comment-bubble-count-container-height)">
  Sets the comment bubble avatar size
</ResponseField>
<ResponseField name="--velt-comment-bubble-count-container-width" default="32px">
  Sets the comment bubble count container width
</ResponseField>

</Tab>

<Tab title="Tool">
## 1) Select the Comment Pin component

You can select the Comment Pin component.
Our CSS variables are set at the component level.

## 2) Modify a CSS variable
Set the variable to anything you want.
We expose a set of variables so you can customize the component to better match your UI.

## 3) Check out the table below

Reference the table below to see what parts we expose.
Alternatively, you can directly inspect the component HTML to see what parts are available.


### Light Mode

<ResponseField name="--velt-comment-tool-icon-size" default="2rem">
  Sets the comment tool icon size
</ResponseField>
<ResponseField name="--velt-comment-tool-border-radius" default="50px">
  Sets the comment tool border radius
</ResponseField>
<ResponseField name="--velt-comment-tool-icon-color" default="#8F95B2">
  Sets the comment tool icon color
</ResponseField>
<ResponseField name="--velt-comment-tool-hover-bg-color" default="#EAE8F9">
  Sets the comment tool hover background color
</ResponseField>
<ResponseField name="--velt-comment-tool-active-fill-color" default="white">
  Sets the comment tool active fill color
</ResponseField>
<ResponseField name="--velt-comment-tool-active-bg-color" default="var(--velt-purple)">
  Sets the comment tool active background color
</ResponseField>


### Dark Mode

<ResponseField name="--velt-comment-tool-icon-size" default="2rem">
  Sets the comment tool icon size
</ResponseField>
<ResponseField name="--velt-comment-tool-border-radius" default="50px">
  Sets the comment tool border radius
</ResponseField>
<ResponseField name="--velt-comment-tool-icon-color" default="#EAE8F9">
  Sets the comment tool icon color
</ResponseField>
<ResponseField name="--velt-comment-tool-hover-bg-color" default="#8F95B2">
  Sets the comment tool hover background color
</ResponseField>
<ResponseField name="--velt-comment-tool-active-fill-color" default="white">
  Sets the comment tool active fill color
</ResponseField>
<ResponseField name="--velt-comment-tool-active-bg-color" default="var(--velt-purple)">
  Sets the comment tool active background color
</ResponseField>


</Tab>
</Tabs>
 
<RequestExample>
```css Pin
snippyly-comment-pin {
  --snippyly-comment-pin-ghost-color: red;
}
```
```css Bubble
snippyly-comment-bubble {
  --snippyly-comment-bubble-container-gap: grey;
}
```
```css Tool
snippyly-comment-tool {
  --snippyly-comment-tool-active-bg-color: grey;
}
```

</RequestExample>