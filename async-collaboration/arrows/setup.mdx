---
title: "Setup"
---

<Tabs>
  <Tab title="React">
    ### 1) Import the `VeltArrowsTool` from `@veltdev/react`
    Import the component

    ### 2) Place the VeltArrowsTool component
    Place the component wherever you want the invite button to appear.

  </Tab>
  <Tab title="Next.js">
    ### 1) Import the `VeltArrowsTool` from `@veltdev/react`
    Import the component

    ### 2) Place the VeltArrowsTool component
    Place the component wherever you want the invite button to appear.


  </Tab>
  <Tab title="HTML">
    ### 1) Import the Velt Script tag
    ```js
    <script type="module" src="https://cdn.jsdelivr.net/npm/@veltdev/sdk@1.0.11/velt.js" onload="loadVelt()"></script>
    ```

    ### 2) Initialize with your API Key

    Put this in your root app script:
    ```js
    async function loadVelt() {
        await Velt.init("YOUR_VELT_API_KEY");
    }
    ```

    ### 3) Place the `<velt-arrows-tool>` component
    Place the component wherever you want the invite button to appear.


  </Tab>
</Tabs>

<RequestExample>

```java React
import { VeltArrowsTool } from '@veltdev/react';

function YourComponent() {

  return (
		...
    <VeltArrowsTool />
    ...
  )
  
}
```

```java Next.js
import { VeltArrowsTool } from '@veltdev/react';

function YourComponent() {

  return (
		...
    <VeltArrowsTool />
    ...
  )
  
}
```

```html HTML
<velt-arrows-tool></velt-arrows-tool>
```

</RequestExample>
