---
title: "Setup"
---

<Tabs>
  <Tab title="React">
    ### 1) Import the `VeltArrowsTool` from `@veltdev/react`
    Import the component

    ### 2) Place the VeltArrowsTool component
    Place the component wherever you want the invite button to appear.

  </Tab>
  <Tab title="Next.js">
    ### 1) Import the `VeltArrowsTool` from `@veltdev/react`
    Import the component

    ### 2) Place the VeltArrowsTool component
    Place the component wherever you want the invite button to appear.


  </Tab>
  <Tab title="HTML">
    ### 1) Import the `<velt-arrows>` component
    Import the component

    ### 2) Place the `<velt-arrows>` component
    Place the component wherever you want the invite button to appear.


  </Tab>
</Tabs>

<RequestExample>

```java React
import { VeltArrowsTool } from '@veltdev/react';

function YourComponent() {

  return (
		...
    <VeltArrowsTool />
    ...
  )
  
}
```

```java Next.js
import { VeltArrowsTool } from '@veltdev/react';

function YourComponent() {

  return (
		...
    <VeltArrowsTool />
    ...
  )
  
}
```

```html HTML
<velt-arrows></velt-arrows>
```

</RequestExample>
