---
title: "Setup"
---

<Tabs>
  <Tab title="React">
    ### 1) Import the `VeltUserRequestTool` from `@veltdev/react`
    Import the component

    ### 2) Place the VeltUserRequestTool component
    Place the component wherever you want the invite button to appear.

  </Tab>
  <Tab title="Next.js">
    ### 1) Import the `VeltUserRequestTool` from `@veltdev/react`
    Import the component

    ### 2) Place the VeltUserRequestTool component
    Place the component wherever you want the invite button to appear.


  </Tab>
  <Tab title="HTML">
    ### 1) Import the `<velt-user-request-tool>` component
    Import the component

    ### 2) Place the `<velt-user-request-tool>` component
    Place the component wherever you want the invite button to appear.


  </Tab>
</Tabs>

<RequestExample>

```java React
import { VeltUserRequestTool } from '@veltdev/react';

function YourComponent() {

  return (
		...
    <VeltUserRequestTool />
    ...
  )
  
}
```

```java Next.js
import { VeltUserRequestTool } from '@veltdev/react';

function YourComponent() {

  return (
		...
    <VeltUserRequestTool />
    ...
  )
  
}
```

```html HTML
<velt-user-request-tool></velt-user-request-tool>
```

</RequestExample>
